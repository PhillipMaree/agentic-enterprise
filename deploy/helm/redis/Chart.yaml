apiVersion: v2
name: platform-redis
description: Redis 8 (bundles RedisJSON + RediSearch natively) for the agentic-platform namespace (dev/kind deploy).
type: application
version: 0.1.0
appVersion: "8"
