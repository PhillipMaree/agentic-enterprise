apiVersion: v2
name: platform-grafana
description: Grafana with pre-provisioned tempo/loki/prometheus datasources for the agentic-platform namespace (dev/kind deploy).
type: application
version: 0.1.0
appVersion: "11.3.0"
dependencies:
  - name: grafana
    version: 8.5.0
    repository: https://grafana.github.io/helm-charts
