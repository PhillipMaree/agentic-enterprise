# Overrides for grafana/grafana, installed directly via `--repo`.
# Mirrors config/grafana/provisioning/datasources/datasources.yaml.
#
# Admin creds come from the platform-grafana-admin Secret (decrypted from
# deploy/sealed-secrets/grafana.sealed.yaml), not inline values.
admin:
  existingSecret: platform-grafana-admin
  userKey: admin-user
  passwordKey: admin-password

service:
  type: ClusterIP

persistence:
  enabled: true
  size: 1Gi

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: "1"
    memory: 1Gi

env:
  GF_FEATURE_TOGGLES_ENABLE: "traceqlEditor traceToMetrics"
  GF_AUTH_ANONYMOUS_ENABLED: "false"
  GF_USERS_ALLOW_SIGN_UP: "false"

datasources:
  datasources.yaml:
    apiVersion: 1
    datasources:
      - name: Prometheus
        uid: prometheus
        type: prometheus
        access: proxy
        # The community prometheus chart exposes a service called
        # `<release>-server` on port 80.
        url: http://platform-prometheus-server
        isDefault: true
        editable: false

      - name: Loki
        uid: loki
        type: loki
        access: proxy
        url: http://platform-loki:3100
        editable: false
        jsonData:
          derivedFields:
            - name: TraceID
              matcherRegex: "trace_id=(\\w+)"
              url: "$${__value.raw}"
              datasourceUid: tempo

      - name: Tempo
        uid: tempo
        type: tempo
        access: proxy
        # The grafana/tempo chart's Service exposes the HTTP query API
        # on port 3100 (chart convention), not Tempo's vanilla 3200.
        url: http://platform-tempo:3100
        editable: false
        jsonData:
          tracesToLogsV2:
            datasourceUid: loki
            spanStartTimeShift: "-5m"
            spanEndTimeShift: "5m"
            filterByTraceID: true
          tracesToMetrics:
            datasourceUid: prometheus
            spanStartTimeShift: "-2m"
            spanEndTimeShift: "2m"
          serviceMap:
            datasourceUid: prometheus
          nodeGraph:
            enabled: true

# Dashboard sidecar: load any ConfigMap in this namespace labelled
# grafana_dashboard=1 as a dashboard.
sidecar:
  dashboards:
    enabled: true
    label: grafana_dashboard
    labelValue: "1"
    searchNamespace: ALL
