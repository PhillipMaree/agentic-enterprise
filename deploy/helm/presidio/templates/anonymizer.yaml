apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-anonymizer
  labels:
    app.kubernetes.io/name: {{ .Release.Name }}-anonymizer
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: anonymizer
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ .Release.Name }}-anonymizer
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ .Release.Name }}-anonymizer
        app.kubernetes.io/instance: {{ .Release.Name }}
        app.kubernetes.io/component: anonymizer
    spec:
      containers:
        - name: anonymizer
          image: "{{ .Values.anonymizer.image.repository }}:{{ .Values.anonymizer.image.tag }}"
          imagePullPolicy: {{ .Values.anonymizer.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 3000
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 30
          resources:
            {{- toYaml .Values.anonymizer.resources | nindent 12 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-anonymizer
  labels:
    app.kubernetes.io/name: {{ .Release.Name }}-anonymizer
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/component: anonymizer
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: http
      name: http
  selector:
    app.kubernetes.io/name: {{ .Release.Name }}-anonymizer
    app.kubernetes.io/instance: {{ .Release.Name }}
