apiVersion: v2
name: platform-tempo
description: Grafana Tempo (single-binary) for the agentic-platform namespace (dev/kind deploy).
type: application
version: 0.1.0
appVersion: "2.6.0"
dependencies:
  - name: tempo
    version: 1.10.0
    repository: https://grafana.github.io/helm-charts
