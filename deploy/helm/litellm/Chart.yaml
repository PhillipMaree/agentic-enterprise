apiVersion: v2
name: platform-litellm
description: LiteLLM proxy with Presidio PII masking for the agentic-platform namespace (dev/kind deploy).
type: application
version: 0.1.0
appVersion: "main-stable"
dependencies:
  - name: litellm-helm
    version: 0.1.781
    repository: oci://ghcr.io/berriai
